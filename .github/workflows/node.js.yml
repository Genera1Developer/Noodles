name: Noodles Deployment

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.x'

      - name: Install dependencies
        run: npm install

      - name: Build Noodles
        run: npm run build

      - name: Deploy Noodles
        run: |
          echo "Deploying Noodles to the dark web... Mayhem commencing!"
          echo "Starting the cascade of digital destruction!"
          # Add your server deployment commands here